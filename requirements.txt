# Core scientific stack
numpy>=1.24
pandas>=1.5
scipy>=1.10
scikit-learn>=1.3
umap-learn>=0.5
statsmodels>=0.14
scikit-posthocs>=0.7

# Plotting
matplotlib>=3.7
seaborn>=0.12

# Imaging / segmentation
scikit-image>=0.21
opencv-python-headless>=4.8
imageio>=2.31
cellpose>=2.3

# (Optional) ML models used in classification
xgboost>=1.7
lightgbm>=4.0

# NOTE:
# - Cellpose needs PyTorch. Install the appropriate wheel for your platform:
#   pip install --index-url https://download.pytorch.org/whl/cu124 torch torchvision torchaudio  # CUDA 12.4
#   pip install torch torchvision torchaudio  # CPU-only (or see pytorch.org)
#
# - tkinter (used for file dialogs) is part of the Python standard library.
#   On Linux you may need the OS package: sudo apt-get install python3-tk
